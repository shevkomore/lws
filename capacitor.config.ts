import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.lws',
  appName: 'lws',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
